**Masthead** — the editorial top bar: serif wordmark left, mono issue line right, hairline rule, frosted-glass backdrop.

```jsx
<Masthead issue={<span><span style={{color:'var(--rt-green)'}}>BP</span> · 2026</span>} />
```

The wordmark pairs the Chinese `裁读` with a small tracked-italic `ReadTailor`. Keep the issue line mono + uppercase. Use it as the fixed cover on landing/reading views.
