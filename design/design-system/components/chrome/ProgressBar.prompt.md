**ProgressBar** — the thin green reading-progress sliver pinned to the viewport top.

```jsx
<ProgressBar value={scrollPct} gradient />
```

2–3px tall, green on transparent. Pass `gradient` for the reading-view treatment. Drive `value` from scroll position.
