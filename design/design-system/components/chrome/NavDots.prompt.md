**NavDots** — the minimal page-dot navigator floating at the landing's bottom.

```jsx
<NavDots count={12} current={page} specialIndex={11} onJump={goTo} />
```

Current dot stretches into a green pill with a soft halo; passed dots fade green; `specialIndex` rotates one into a diamond (the closing vision page). Sits in a frosted pill container.
