**EmptyState** — the empty shelf, in the letter voice: ⌜ ⌟ quote-corners, a short serif line, muted explanation, at most one quiet action. Never an illustration or emoji.

```jsx
<EmptyState title="书架还空着" action={<Button variant="secondary">挑一本书</Button>}>
  挑一本你一直想读、却总是读不完的书。
</EmptyState>
```
